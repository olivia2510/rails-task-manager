<h1>Details</h1>
<h2><%=@task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>

<%= link_to 'Edit a task', edit_path(@task) %>
<%= link_to 'Delete a task', delete_path(@task),
data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%>
